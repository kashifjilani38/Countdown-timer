# Countdown-timer
This Python program is a countdown timer that takes a time duration from the user in seconds and counts down to zero. It uses Python's time module to pause the program for one second between each countdown step. The divmod() function converts the total seconds into minutes and seconds, which are displayed in MM:SS format. 
